[[providers]]
name = "doronic"
type = "postgis"
uri = "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
srid = 4326

    [[providers.layers]]
    name = "occurrences"
    tablename = "species_occurrences"
    geometry_fieldname = "occurrences"
    id_fieldname = "taxon_key"

[[maps]]
name = "occurrences"
tile_buffer = 0

    [[maps.layers]]
    provider_layer = "doronic.occurrences"
